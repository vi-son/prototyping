import React from "react";
import ReactDOM from "react-dom";

const App = () => {
  return <h1>Exhibition Prototype</h1>;
};

const mount = document.querySelector("#mount");
ReactDOM.render(<App />, mount);
